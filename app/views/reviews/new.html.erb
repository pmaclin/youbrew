
<%# @r = Review.find_by({ :batch_id => 'batch.id', :user_id => 'current_user.id' }) %>
<%# if @r.present? %>
<%#= @r.batch.uni.rand_num %>
<%# end %>
<% @u = Uni.find_by({ :rand_num => 'params[:new_number]', :user_id => 'current_user.id' }) %>
<% if @u.present? %>
  <%= uni.rand_num %>
<% end %>

<%# if params[:new_number] == @uni.rand_num and current_user.id == @uni.user_id %>
  <!-- <h4>You can't review one of your own batches. C'mon man.</h4> -->

<%# else %>

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">New Review for batch/brew id# <%= @review.batch_id %> | <%= @review.batch.recipe.name %></h3>
      </div>
      <div class="panel-body">
        <%= render 'form', review: @review %>
      </div>
    </div>
  </div>
</div>

<%# end %>




<%# if params[:new_number] == review.batch.uni.rand_num %>
<!-- <h4>This batch has already been reviewed.</h4> -->
<%# end %>

<!-- if there's a row where the params of new_number match the uni.rand_num, and the uni.user_id match the current user, then they should not be
able to do the review. -->


